diff --git a/dist/components/CodeView.js b/dist/components/CodeView.js
index 2fd5213232a3c550d483dcdf61497ed40f070a9b..24d68c9e7446934de7651c3ad15e0de4deb516dd 100644
--- a/dist/components/CodeView.js
+++ b/dist/components/CodeView.js
@@ -1280,6 +1280,12 @@ var CodeView = class CodeView {
 			firstDirtyIndex = Math.max(nextItems.length - 1, 0);
 		}
 		this.capturePendingLayoutAnchor(nextIdToItem);
+		// The rendered range belongs to the old order. Release it before replacing
+		// the list, or the next render will clean up the wrong records by index.
+		if (previousItems.some((item, index) => nextItems[index] !== item)) {
+			this.cleanAllRenderedItems();
+			this.resetRenderState();
+		}
 		for (let index = 0; index < previousItems.length; index++) {
 			const removedItem = previousItems[index];
 			if (removedItem == null || !removedItems.has(removedItem)) continue;
